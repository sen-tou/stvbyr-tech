{
  "name": "yerstem",
  "version": "0.0.1",
  "description": "Yerstem website",
  "main": "index.js",
  "scripts": {
    "dev": "hugo server -D --disableFastRender",
    "build": "cross-env NODE_ENV=production hugo --verbose --gc --minify",
    "build:example": "cross-env NODE_ENV=production hugo --verbose --gc --minify --baseURL http://yerstem.project",
    "start": "hugo --gc"
  },
  "author": "Steve Beyer",
  "license": "Propritary",
  "dependencies": {
    "@tailwindcss/aspect-ratio": "^0.2.0",
    "autoprefixer": "^10.1.0",
    "cross-env": "^7.0.3",
    "in-viewport": "^3.6.0",
    "postcss": "^8.2.1",
    "postcss-cli": "^8.3.0",
    "postcss-import": "^13.0.0",
    "tailwindcss": "^2.0.2"
  },
  "browserslist": [
    "last 1 version",
    "> 1%",
    "maintained node versions",
    "not dead"
  ],
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.10"
  }
}
